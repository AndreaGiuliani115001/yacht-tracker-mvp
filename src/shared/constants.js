// src/shared/constants.js

/** Colori per le classificazioni (riusati in map, charts, table) */
export const CLASS_COLORS = Object.freeze({
    verde: "green",
    giallo: "gold",
    rosso: "red",
});

/** Locale e formati tempo di default (se vuoi centralizzarli qui) */
export const DATETIME_LOCALE = "it-IT";
