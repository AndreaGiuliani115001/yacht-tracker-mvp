/** Costanti UI per i grafici */
export const CHART_DEFAULTS = Object.freeze({
    HEIGHT: 300,
    GRID_DASH: "3 3",
});

// src/shared/constants.js
export const CHART_ANIM = Object.freeze({
    ENABLED: false,       // metti false se vuoi spegnere tutti
    DURATION: 300,       // ms
    EASING: "linear",    // "linear" è più fluido su stream
});